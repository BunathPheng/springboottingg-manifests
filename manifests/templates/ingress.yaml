---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboottingg                  # Ingress name
  labels:
    app: springboottingg
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springboottingg.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springboottingg               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
